<?php

return [

    /*
    |--------------------------------------------------------------------------
    | JWT Secret Key
    |--------------------------------------------------------------------------
    | Used to sign tokens with HMAC-SHA256. Generate with:
    |   php artisan jwt:secret   (or set JWT_SECRET manually in .env)
    */
    'secret' => env('JWT_SECRET'),

    /*
    |--------------------------------------------------------------------------
    | Token Lifetimes (in minutes)
    |--------------------------------------------------------------------------
    */
    'ttl'         => env('JWT_TTL', 60),           // Access token: 60 min
    'refresh_ttl' => env('JWT_REFRESH_TTL', 10080), // Refresh token: 7 days

    /*
    |--------------------------------------------------------------------------
    | Algorithm
    |--------------------------------------------------------------------------
    */
    'algo' => 'HS256',

    /*
    |--------------------------------------------------------------------------
    | Issuer / Audience
    |--------------------------------------------------------------------------
    */
    'issuer'   => env('APP_URL', 'http://localhost'),
    'audience' => env('APP_URL', 'http://localhost'),

];
